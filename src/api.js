import api, { getToken, setAuthSession, removeAuthSession, getCurrentUser, isAuthenticated } from './lib/api';

export { getToken, setAuthSession, removeAuthSession, getCurrentUser, isAuthenticated };
export default api;
